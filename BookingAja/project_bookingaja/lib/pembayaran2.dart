import 'package:flutter/material.dart';

class pembayarann extends StatelessWidget {
  const pembayarann({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Pembayaran"),
      ),
      body: Center(
        child: Text("Belum Ada Data"),
      ),
    );
  }
}
